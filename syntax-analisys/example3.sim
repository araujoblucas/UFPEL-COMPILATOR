main {
    var bool isGreater;
    var int number;

    while((isGreater == false)) do {
        if ((number > 100)) {
            isGreater := true;
        };
        number := (number + 1);
    };
    return number;   
    
}