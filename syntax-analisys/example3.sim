main {
    var bool isGreater;
    var int number;

    while((isGreater == false)) do {
        if ((number > 100)) {
            isGreater := true;
        };
        number := (number + 1);
    };
    return number;   
    
}

function int whatToDo(int dayOfWeek) {
    if( ((dayOfWeek == 7) || (dayOfWeek == 1)) ) {
        return party;
    };
    return (work && study);
}