main
{
    var int n;
    n := (n - 10);
    do {
        check(a);
        doubleCheck(a);
    } while ( (n > 10) );
    print(a);
}

function int check(int t) {
    if((t == true))
    {
        return 3;
    };

        return (3 * 25);
}

function int doubleCheck(int x) {
    return x;
}